{
  "name": "@tu-examples/tailwind",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tu + Tailwind v4 — utility-first styling alongside Tu's scoped style blocks.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo 'no tests'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-lang/runtime": "workspace:*"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@tu-lang/compiler": "workspace:*",
    "@tu-lang/vite": "workspace:*",
    "tailwindcss": "^4.0.0",
    "vite": "^7.0.0"
  }
}
