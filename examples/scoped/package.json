{
  "name": "@tu-examples/scoped",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "M1.8 demo: two components declaring the same `.card` selector — verifies scoping prevents collision.",
  "scripts": {
    "build": "echo 'scoped example: visual demo only — exercise it via the playground'",
    "test": "echo 'no tests'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-ui/compiler": "workspace:*",
    "@tu-ui/runtime": "workspace:*"
  }
}
