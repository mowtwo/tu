{
  "name": "@tu-examples/js-compat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "JS/TS compatibility showcase — async/await, try/catch/finally, ternary, template literals, spread, optional chaining, nullish coalescing, regex literal, update operators, external JS escape hatch.",
  "scripts": {
    "demo": "node run.mjs",
    "build": "echo 'js-compat example: built on demand by run.mjs'",
    "test": "echo 'no tests'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-lang/compiler": "workspace:*",
    "@tu-lang/dom": "workspace:*",
    "@tu-lang/runtime": "workspace:*",
    "@tu-lang/std": "workspace:*",
    "jsdom": "^26.0.0"
  }
}
