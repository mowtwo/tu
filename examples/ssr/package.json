{
  "name": "@tu-examples/ssr",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "M4 V1 demo: SSR → hydrate round-trip. Server renders Counter.tu to HTML; client adopts that DOM via hydrate() and wires up interactivity.",
  "scripts": {
    "demo": "node run.mjs",
    "build": "echo 'ssr example: built on demand by run.mjs'",
    "test": "echo 'no tests'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-lang/compiler": "workspace:*",
    "@tu-lang/dom": "workspace:*",
    "@tu-lang/runtime": "workspace:*",
    "jsdom": "^26.0.0"
  }
}
