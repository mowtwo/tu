# Example: Counter

The canonical demo for Tu — a `+ / −` counter showing top-level `let` reactivity, `computed`, conditional rendering, and a unified style block.

> **Status**: M0 placeholder. Source `Counter.tu` is added when the language can compile (M1+).
