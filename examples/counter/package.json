{
  "name": "@tu-examples/counter",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "M1.2 demo: reactive counter with state + computed cells.",
  "scripts": {
    "demo": "node run.mjs",
    "build": "echo 'counter example: built on demand by run.mjs'",
    "test": "echo 'no tests'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-lang/compiler": "workspace:*",
    "@tu-lang/runtime": "workspace:*"
  }
}
