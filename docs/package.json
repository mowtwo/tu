{
  "name": "tu-docs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tu (图) documentation site — built with @tu-lang/tu-shu (Tu's own SSG, dogfood).",
  "scripts": {
    "build": "node ../packages/tu-shu/dist/cli.js build",
    "dev": "echo 'tu-shu dev server is not yet implemented; rebuild with: pnpm build'",
    "preview": "echo 'serve docs/.tu-shu/dist via any static file server'",
    "test": "echo 'no tests — static site'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-lang/tu-shu": "workspace:*"
  }
}
