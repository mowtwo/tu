{
  "name": "tu-playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "In-browser playground for Tu — bundles every milestone demo via the @tu/vite plugin.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo 'no tests — playground is a runtime app'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu/runtime": "workspace:*"
  },
  "devDependencies": {
    "@tu/compiler": "workspace:*",
    "@tu/vite": "workspace:*",
    "vite": "^7.0.0"
  }
}
