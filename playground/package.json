{
  "name": "tu-playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "In-browser playground for Tu — bundles every milestone demo via the @tu-lang/vite plugin.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "echo 'no tests — playground is a runtime app'",
    "check": "echo 'no checks'"
  },
  "dependencies": {
    "@tu-lang/dom": "workspace:*",
    "@tu-lang/runtime": "workspace:*",
    "@tu-lang/std": "workspace:*",
    "@tu-lang/tu-xing": "workspace:*",
    "monaco-editor": "^0.55.1"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@tu-lang/compiler": "workspace:*",
    "@tu-lang/vite": "workspace:*",
    "tailwindcss": "^4.0.0",
    "vite": "^7.0.0"
  }
}
