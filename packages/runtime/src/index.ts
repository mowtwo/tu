export const VERSION = '0.0.0'

/**
 * Placeholder for the Tu runtime entrypoint.
 * The real Signal cell, render(), and reconcile() implementations land in M1.
 */
export const RUNTIME = '@tu/runtime'
