/**
 * Static extension metadata. Lives in a separate file (no `vscode` import)
 * so tests can pull these values without the LSP/extension runtime
 * machinery being resolvable.
 */
export const VERSION = '0.0.1'
export const EXTENSION_NAME = 'vscode-tu'
